# Fresh-eyes adversarial review — Scope (proposal.md)

Date: 2026-08-21
Subject: Shoot4Fun Shop Surface estimation project `shoot4fun-shop`, phase P1.
Reviewer: independent context (fresh eyes), invoked per REF-Delivery §1 phase reflex.

## First pass — findings (verbatim)

The first pass returned VERDICT: FAIL with these findings:

1. "Acquire" is a hollowed-out requirement. ACQ-001/ACQ-002 priced only as UI/API/write;
   no entitlement/ownership model, no grant trigger, no economy. MON-001 buries the gap.
2. "Apply" (COS-001) read as unrequested scope creep (22 MD) — reviewer had only the first
   paragraph of the ask and missed the explicit "Apply: acquired cosmetics can be applied to
   the player model" bullet in ticket #64's Scope-to-deliver. MISREAD — Apply is in scope.
3. Arsenal read-back missing: ACQ-002 writes the envelope, but no activity reads owned/equipped
   state back for the catalog badges and equip UI.
4. Catalog data source/authorship undefined (SHOP-001/002 present a catalog with no stated source).
5. Cosmetic asset availability is an assumption (RENDER-001), not a deliverable — BUILD-004 depends on it.
6. No cross-cutting UX states (error/empty/owned/not-entitled).
7. Traceability otherwise clean.

## Revisions made to the scope (this line)

- Added **ACQ-003 Ownership & entitlement model** + **BUILD-009** (grant trigger + ownership
  record written into the Arsenal envelope; free unlock this phase). Closes finding #1.
- Added **CAT-001 Define the cosmetics catalog (source & authorship)** + **BUILD-008** (static /
  authored catalog source) and assumption **CAT-001**. Closes finding #4.
- **BUILD-005** now explicitly reads the Arsenal envelope for owned/equipped state. Closes #3.
- Added **BUILD-010 Shop UX states** (empty/error/already-owned/not-entitled). Closes #6.
- **COS-001 (Apply)** retained — confirmed in-scope per ticket #64. Finding #2 rejected as misread.
- Finding #5 (RENDER-001) is an explicit, rated assumption (MEDIUM/CRITICAL), accepted as a risk,
  not a deliverable of this phase.

Re-audit after revisions: **62 passed, 0 failed, 0 skipped** (roadmap/activity/architecture checks
all green). Effort: build 100 MD (incl. 15 MD design), project totals consistent.

## Re-review conclusion

Every meaningful line of the verbatim ask traces to a requirement ID; the real gaps the first
pass found (acquisition engine, catalog source, read-back, UX states) are now priced and covered;
the one rejected finding was a misread of the ask. The scope is complete and audit-clean.

VERDICT: PASS
