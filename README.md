# Delivery scratch

Committed on this line's own `delivery/2026-08-21` branch and mounted at `.delivery/` per
REF-Delivery. Progress is derived from which phase records exist here. Phase: build+ship
(degraded — parked at the production guest-mint fault; `build.md`'s round-3 section names
the cause and the re-drive that clears it).
