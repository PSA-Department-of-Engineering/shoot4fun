shoot4fun:
  images:
    server:
      tag: 1.16.5
      repository: ghcr.io/psa-department-of-engineering/shoot4fun-server
    client:
      tag: 1.16.5
      repository: ghcr.io/psa-department-of-engineering/shoot4fun-client
    docs:
      tag: 1.16.5
      repository: ghcr.io/psa-department-of-engineering/shoot4fun-docs
  database:
    enabled: true
    secret: pg-app-shoot4fun
  # System test accounts (carter, katael): the backend pins them to this
  # password at every boot, so the admin password always opens both (#72).
  systemAccounts:
    password: psaadmin
