# Shoot4Fun — prod environment (deploy-chaos)
# The image-updater writes <app>.images.<component>.tag here on each release.
paused: false

shoot4fun:
  images:
    server:
      tag: "1.0.2"
    client:
      tag: "1.0.2"
    docs:
      tag: "1.0.2"

server:
  image:
    tag: "1.0.2"
  replicas: 1

client:
  image:
    tag: "1.0.2"
  replicas: 1

leaderboard:
  database:
    enabled: false
    secretName: pg-app-shoot4fun
