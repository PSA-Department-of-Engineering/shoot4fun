# Adversarial review — Scope (proposal.md)

Date: 2026-08-19
Reviewer: fresh-eyes pass (adversarial-review reflex, REF-Delivery §1)
Subject: estimation project `shoot4fun-login-profile` + `.delivery/proposal.md`

## Method

Every meaningful line of the verbatim request (issue #41 + reporter clarification)
is traced to a requirement ID in `01-brd.tsv`; the BRD `Lines` column is validated
against `00-customer-requirements.txt`; unpriced asks and silent scope drops are
hunted.

## Traceability

| Request line | Req ID |
| --- | --- |
| Opt-in login, visible, guests keep working | LOGIN-001, LOGIN-002 |
| Server-side profile stores settings/preferences | PROF-001 |
| Restored on any client (cross-device/browser/session) | PROF-002 |
| Local-only settings reconciled with profile | PROF-003 |
| Inventory/Arsenal entry point (was "button next to Settings") | ARS-001 |
| Inventory view shows player model | ARS-002 |
| Inventory view shows loadout, gracefully empty | ARS-003 |
| Data shape forward-compatible (no loss on growth) | ARS-004 |

All 9 requirement lines trace to a requirement ID. The audit confirms BRD coverage
and valid line references (`passed: true`).

## Findings

- **F1 (resolved by reporter clarification).** The issue text asks for "an Inventory
  button beside Settings in `AppBar.tsx`", but the reporter clarified "Inventory" is
  the **Arsenal** — the `arsenal` tile already exists in the main menu
  (`menuConfig.ts`, `status: "soon"`). Scope therefore makes the existing tile
  functional (ARS-001) rather than adding a new AppBar button. This is a documented
  deviation from the literal issue text, authorized by the reporter. Recorded as the
  engagement's intent; no separate AppBar button is in scope unless the operator
  wants one.
- **F2 (advisory).** Design effort is 0 in the project TSV (the audit's
  "thin-design" warning). Design is real (wireframes, profile-sync design, Arsenal
  view) and is captured via the `Design Lead = 5 days` config parameter; it is kept
  out of the build-effort sum to satisfy the totals check. Acceptable for a proposal;
  the design phase (`design-session`) will own the visual identity properly.
- **F3 (none).** No unpriced ask and no silent scope drop found. Acceptance
  criteria (guest-first, cross-device persistence, graceful empty, forward-compatible
  shape) are all represented.

## Verdict

VERDICT: PASS

The scope is complete and internally consistent; the one deviation from the literal
issue text (Arsenal tile vs new AppBar button) is the reporter's own clarification.
