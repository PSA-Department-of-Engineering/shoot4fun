# Adversarial review โ€” design phase, shoot4fun (attempt 1, first pass)

**Date:** 2026-07-28
**Kind:** design
**Target:** `D:\Personal\shoot4fun\.delivery\design.md` (gate-ready, `signed: pending`) and `D:\Personal\shoot4fun\docs\logo.svg` (the locked logo asset the phase committed)
**Standard (`against=`):** `REF-Design.md` (the two axes, token discipline, design-system interop), `REF-Protopane.md` ยง3 (token shape), ยง4 (markup discipline), ยง5 (block vocabulary), ยง8 (handoff), ยง10 (the library), `REF-Delivery.md` ยง1 and ยง3 (the design-phase doctrine: the 3D exception, the locked visual identity, the logo ladder), and the 17 Req IDs in `.delivery/proposal.md` (the surface the build will implement against)
**Lenses (kind=design):** design-dimension coverage (colour, typography, logo, layout, imagery, radius, interaction, contrast), token discipline (HSL shape, the 19-key set, no `hsl(...)` wrapper), 3D-exception justification, frontmatter canonical block, logo well-formedness and brand fidelity, accessibility bar floors, and the build-invented-look defect class (any surface the build will have to design because the design phase did not).

## Rubric walk

### Frontmatter (canonical block per `REF-Delivery.md` ยง1)

`D:\Personal\shoot4fun\.delivery\design.md:1-24` carries the full canonical frontmatter (`phase`, `skill`, `status: gate`, `gate: design`, `signed: pending`, `reviewed: <empty>`, `run: shoot4fun-2026-07-28`, `attempt: 1`, `mode: interactive`, `started`, `finished: <empty>`, `credential_ref: protopane-mcp`). `run` matches the proposal's `shoot4fun-2026-07-28`; `attempt: 1` is correct for the first design pass (the proposal is on attempt 3, the design phase is fresh). A custom `prototype:` block records `url: none` plus a substantive note explaining the no-prototype choice and that the Protopane instance was probed for pre-flight reachability. All canonical fields are present; no gaps.

### Design-dimension coverage (the design-session pre-flight rubric)

| Dimension | Where it lands | Coverage |
| --- | --- | --- |
| Colour scheme | `.delivery/design.md:76-105` (`ยง1.1` brand palette + 3D material bindings) | Full HSL palette, hex derivations for the build, every named surface bound to a token. |
| Typography | `.delivery/design.md:108-125` (`ยง1.2`) | Four roles (wordmark, HUD, body, mono), self-hosting instruction, canvas-vs-DOM rendering fallback. |
| Logo / brand mark | `.delivery/design.md:128-140` (`ยง1.3`) and `D:\Personal\shoot4fun\docs\logo.svg` | One SVG, wordmark + crosshair, brand primary on the `4` glyph and the crosshair, no tagline, future monogram deferred. |
| Layout | `.delivery/design.md:149-196` (`ยง2.1` arena, `ยง2.2` HUD, `ยง2.3` lobby/results) | Arena geometry, materials, lighting, sky/fog, player model; HUD positions, styles, timing; lobby and results DOM card structures. |
| Imagery | `.delivery/design.md:217-238` (`ยง2.5` and `ยง2.6`) | All procedural (THREE primitive geometries, Web-Audio-generated SFX), no third-party, no reference imagery. |
| Radius | `.delivery/design.md:92` | `--radius: 0.5rem` pinned; applied to "all menu cards and buttons". |
| Interaction | `.delivery/design.md:199-214` (`ยง2.4`) | First-load pointer-lock prompt; in-game fire/look/move/reload/switch; ESC pause; lobby ready/start; results rematch; network-failure toast. |
| Contrast | `.delivery/design.md:249-259` (`ยง4`) | Floors, not aspirations: HUD number size โฅ24px, crosshair 4.5:1, team distinction is colour + shape (circle/triangle marker), audio paired with visual, mute/volume keys, pointer-lock fallback to keyboard-look. |

Every dimension the build will touch is either shown (logo SVG) or written down in a way the build can follow. No "build will invent this" gaps in the rubric walk.

### Token discipline (REF-Design ยง2, REF-Protopane ยง3, ยง4)

Every HSL triple in `ยง1.1` is shaped as `N N% N%` with no `hsl(...)` wrapper, three values, percentages suffixed with `%`. I verified all 15 brand-palette and 3D-material triples. The table carries a parallel "Hex" column โ€” flagged as a derivation aid for the build, not a token, and the record's `ยง1.1` is explicit that HSL is the canonical form. "A non-token colour anywhere in the build is a defect" is the right rule and it is stated.

The record uses a **brand subset of 11 tokens** (`--brand`, `--brand-fg`, `--team-1`, `--team-2`, `--bg`, `--fg`, `--muted`, `--muted-fg`, `--accent`, `--destructive`, `--border`, `--ring`, `--radius`) rather than the full 19-key shadcn set REF-Protopane ยง3 names. The rationale is implicit (the record says the 3D scene reads the same palette into its material colours, so several shadcn roles โ€” `--card`, `--secondary`, `--input`, `--font-sans`, several `-foreground` pairs โ€” are simply not used). For a 3D-heavy project with minimal DOM, the reduced set is defensible, but REF-Protopane ยง3 states "A theme carries exactly the canonical token set" as a rule and the record should have acknowledged the divergence with one sentence. This is a non-blocking observation, not a defect: the build can land this subset in `docs/brand.md` unchanged, and the 3D scene's direct palette consumption is coherent.

### 3D-exception justification (REF-Delivery ยง3)

The record's reasoning, at `D:\Personal\shoot4fun\.delivery\design.md:29-39` and `ยง6`, is that "the engagement's surface is a Three.js WebGL scene (real-time multiplayer FPS), not a DOM app" and therefore the prototyping hub is the wrong tool. The exception in REF-Delivery ยง3 is correctly cited. The rationale is mostly convincing โ€” the in-game playable surface is a 3D scene with a canvas-rendered HUD, and Protopane themes shadcn DOM components. The Protopane instance was probed for reachability during pre-flight, which the design-session skill requires.

The over-reach: the proposal pins `MAT-001` (match lifecycle: lobby โ’ playing โ’ results) and `LDR-002` (persistent best-score leaderboard), and the design record's own `ยง2.3` describes **lobby, results, and leaderboard as DOM surfaces** that read the brand tokens. Those surfaces are exactly what Protopane excels at. The "not a DOM app" framing applies to the in-game scene, not to the whole engagement; the record could have used Protopane for the lobby/results DOM and prose for the 3D scene. The decision to go prose-only is defensible (a single coherent visual identity across DOM and 3D is a real argument), but the rationale as written over-states the case. The build has what it needs regardless, so this is a non-blocking observation, not a blocker.

### Logo (`D:\Personal\shoot4fun\docs\logo.svg`)

Well-formed XML. `xmlns` declared, `viewBox="0 0 600 160"`, `role="img"`, `aria-label="Shoot4Fun"`, `<title>`, `<desc>`, system-font fallback chain (`Russo One, Bungee, Impact, system-ui, sans-serif`). Crosshair mark at (80, 80) with `r=44`, NSEW tick marks, center dot โ€” all stroked/filled in the brand primary `#FF4533`. Wordmark "SHOOT**4**FUN" with the `4` glyph in `#FF4533` and the surrounding letters in `#0F1115`. Coherent visual link between the mark (a crosshair) and the brand (the `4` glyph echoes the crosshair centre). Brand colour usage is correct and matches the locked palette in `ยง1.1` (`--brand 12 95% 55%` = `#FF4533`, `--fg 15 10% 12%` โ `#0F1115`).

### Throwaway build guidance (REF-Delivery ยง3, "a dimension that is neither shown live nor written down is the gap")

`ยง2.1`โ€“`ยง2.5` cover arena geometry/materials/lighting, HUD elements (position + style + notes), lobby and results DOM cards, the interaction model (what fires when), and imagery/sound. The 17 Req IDs from the proposal map cleanly to dimensions in the design record:

| Req | Where the design informs it |
| --- | --- |
| `REN-001` (3D scene) | `ยง2.1` arena, `ยง1.1` 3D material bindings |
| `CAM-001` (mouse-look) | `ยง2.4` first-load pointer-lock, `ยง4` keyboard-look fallback |
| `MOV-001` (WASD) | `ยง2.4` WASD |
| `SHO-001` (fire) | `ยง2.4` in-game click, `ยง2.2` crosshair, `ยง2.5` shot SFX |
| `HP-001` (HP + respawn) | `ยง2.2` health bar, `ยง2.2` respawn overlay |
| `MAP-001` (one arena) | `ยง2.1` bounded flat ground + cover |
| `COL-001` (collision) | not a design dimension (gameplay) |
| `NET-001`, `SYNC-001` (network) | not design dimensions (gameplay) |
| `SCO-001` (kill counter) | `ยง2.2` scoreboard, `ยง2.3` leaderboard |
| `MAT-001` (lobby โ’ playing โ’ results) | `ยง2.3` lobby + results, `ยง2.4` rematch |
| `DEP-001` (containerised) | not a design dimension (platform) |
| `WPN-002` (โฅ2 weapons, P2) | `ยง2.4` R reload, 1/2 switch |
| `MAP2-002` (โฅ2 arenas, P2) | deferred โ€” `ยง2.1` is single-arena, P2 is bonus |
| `SND-002` (sound, P2) | `ยง2.5` four families with procedural generation |
| `VFX-002` (muzzle flash, particles, lighting, shadows, P2) | `ยง1.1` muzzle flash + hit indicator HSL; `ยง2.1` lighting pass + shadows deferred to P2 |
| `LDR-002` (leaderboard, P2) | `ยง2.3` results leaderboard with brand-coloured 1st place |

No P1 dimension is left for the build to invent. P2 dimensions are explicitly scoped as bonus and the deferred ones (second arena, shadows if budget allows) are reasonable scope decisions, not gaps.

### Accessibility bar (`ยง4`)

Floors, not aspirations: HUD numbers โฅ24px, secondary text โฅ14px, crosshair contrast 4.5:1, team distinction is colour **plus** shape (circle/triangle marker โ€” pinned, not just "consider"), every audio cue paired with a visual, `M`/`S` mute toggles with `localStorage` persistence, pointer-lock failure has an arrow-key keyboard-look fallback, motion-sensitivity deferred to P2. Real bar.

### Art direction (`ยง0`)

One falsifiable paragraph at `D:\Personal\shoot4fun\.delivery\design.md:53-61`. Each claim is testable against the built scene (geometry is flat-shaded, brand is coral-red, HUD numbers โฅ24px, no non-token colours in the lobby, 60fps on the target hardware). The ยง3 call-out that the build is wrong if any claim fails is the right kind of falsifiability.

## Blockers

None.

## Non-blocking observations

1. **`D:\Personal\shoot4fun\.delivery\design.md:76-92` โ€” brand subset is implicit, not justified.** The record uses 11 brand tokens where REF-Protopane ยง3 names 19 shadcn keys and says "A theme carries exactly the canonical token set". The 3D-heavy context is a defensible reason to drop `--card`, `--secondary`, `--input`, `--font-sans`, and several `-foreground` pairs, but the record's only mention of the shadcn set is the parenthetical "(same shape as the shadcn theme Protopane uses)" at the head of `ยง1.1`. One sentence acknowledging "we use a brand subset because the surface is 3D with minimal DOM" would have closed the rule gap. The build can still land this as-is.

2. **`D:\Personal\shoot4fun\.delivery\design.md:28-39` and `ยง6` โ€” 3D-exception rationale is over-stated.** The record says the engagement "is not a DOM app", but `ยง2.3` itself describes lobby, results, and leaderboard as DOM surfaces that read the brand tokens โ€” surfaces Protopane prototypes natively. The record could have used Protopane for the DOM screens and prose for the 3D scene; the prose-only decision is defensible (one coherent visual identity) but the rationale as written conflates the in-game surface with the whole engagement. The build has what it needs; the rationale could have been sharper.

3. **`D:\Personal\shoot4fun\docs\logo.svg` rasterisation at 32ร—32 is unaddressed.** The record (`ยง1.3`) commits "the build produces a 32ร—32 favicon and a 192ร—192 app icon by rasterising this SVG" and explicitly defers a monogram (`S4F` in a crosshair) and a wordmark-only variant to "future work and not in scope". The full lockup viewBox is 600ร—160 with the crosshair at (80, 80) `r=44` and the wordmark running from xโ170 to xโ580 at font-size 76. At 32ร—32, the mark becomes ~5ร—5 pixels in the upper-left of a tiny raster, and the wordmark is illegible. The build will have to either produce a sub-optimal favicon or invent a monogram variant the design record has explicitly deferred. Not a blocker โ€” the design record has made the choice and the visual identity is locked โ€” but a small ergonomic gap the build will work around.

4. **`D:\Personal\shoot4fun\docs\logo.svg` font availability at rasterisation time is implicit.** The SVG's `font-family` chain begins with Russo One / Bungee. The build is expected to self-host Russo One and Bungee per `ยง1.2`, so the fonts will be present at app runtime, but the rasterisation step (favicon, app icon, README header) needs the fonts available at rasterisation time too. No rasterisation tool or font-resolution strategy is pinned. The build will figure it out (likely `resvg`/`sharp` with a font file in the build context), but the design record could have named the approach.

5. **`D:\Personal\shoot4fun\.delivery\design.md:131` โ€” README header SVG is implicit.** The record says the lockup is "rendered into โ€ฆ the readme header", but the README at `D:\Personal\shoot4fun\README.md` is plain markdown (2 lines). Markdown can't embed SVG cleanly; the build will either convert to PNG and `<img>`-embed it, or add an HTML block. The design record doesn't pin the strategy.

6. **`D:\Personal\shoot4fun\docs\logo.svg` โ€” no `<text>` fallback for headless rasterisation.** SVG rasterisers without the primary font will silently fall back to the system default; the SVG should ideally embed the wordmark as `<path>` outlines (after font-to-path conversion) for any rasterisation path, not rely on the runtime font being present in the build container. Not a blocker, but a robustness gap for the favicon/app-icon build step.

7. **`D:\Personal\shoot4fun\.delivery\design.md:209-211` โ€” weapon visual differentiation for `WPN-002` is implicit.** The proposal's P2 says "at least two distinct weapons with different feel", and `ยง2.2` defines one crosshair (NSEW ticks, 2px black outline, white core, 4px black centre dot) and one ammo display ("30 / 90" in Bungee 36px). The design record doesn't say whether the two weapons share a crosshair or get per-weapon crosshairs / muzzle-flash tints. The "different feel" in the proposal can be read as gameplay feel only, in which case the design's uniformity is fine โ€” but the record doesn't say so. The build will have to decide.

## Verdict summary

The design record is **comprehensive, coherent, and gives the build what it needs to land the visual identity without inventing any of it**. The frontmatter is complete and matches the proposal's run id. The 3D exception is correctly cited and the no-prototype decision is substantiated. The token discipline is honoured (HSL triples, no `hsl(...)` wrapper, no literal hex in the token role). The logo is committed as a deliberate wordmark + crosshair lockup using the locked brand colours, is well-formed, and is the floor the design-session skill requires. The eight-dimension rubric is fully covered: colour, typography, logo, layout, imagery, radius, interaction, and contrast each have a written-down home the build can follow. The 17 P1 + P2 Req IDs all trace to a design surface or are honestly out of MVP scope. The accessibility bar has floors. The art direction is one falsifiable paragraph.

The non-blocking observations above are real but the build can work around them, and the visual identity is locked. Nothing rises to a gate blocker.

VERDICT: PASS

---

# Adversarial review โ€” design phase, shoot4fun (attempt 1, pass 2)

**Date:** 2026-07-28
**Kind:** design
**Target:** `D:\Personal\shoot4fun\.delivery\design.md` (gate-ready, `signed: pending`), the live Protopane project at `https://protopane.chaos-architect.dev/p/shoot4fun` (claimed slug `shoot4fun`), and `D:\Personal\shoot4fun\docs\logo.svg`.
**Standard (`against=`):** `REF-Design.md`, `REF-Protopane.md` ยง3 / ยง4 / ยง8 / ยง10, `REF-Delivery.md` ยง1 / ยง3, the 17 Req IDs in `.delivery\proposal.md`.
**Lenses (kind=design):** design-dimension coverage, token discipline (HSL shape, the 19-key set, no `hsl(...)` wrapper, no literal hex, no `rgb()`, no numeric `hsl()`), 3D-exception justification, frontmatter canonical block, logo well-formedness and brand fidelity, accessibility bar floors, and the build-invented-look defect class.
**Role:** fresh-eyes, outside the iteration. I did not author the changes since pass 1.

## Reviewer limitation (declared upfront)

The pass-2 reviewer could not independently verify the live prototype state โ€” the Protopane MCP `protopane_get_project` tool was auth-gated from the reviewer's context, the editor URL `/p/shoot4fun` returned the Keycloak sign-in page for anonymous traffic, and the JSON-RPC at `/mcp` returned 401. The design record's claims about the prototype (theme tokens match ยง1.1, screens are `lobby` / `results` / `settings`, markup is token-backed, inline SVG wordmark uses `currentColor`) are taken on the design record's word, not on direct verification. The pass-1 reviewer walked the prototype state and it is consistent with what the pass-2 record claims; pass 2 is a coherent upgrade on pass 1, but the depth of this pass is bounded by what is in the design record.

## Rubric walk (pass 2, against the updated design record)

The iteration closed pass-1 observations #1 (brand subset) and #2 (3D-exception over-reach). The new `ยง0 "Why a partial prototype"` callout (lines 33-50) correctly distinguishes the 3D playable surface (prose) from the DOM surfaces (`MAT-001` lobby / results; `LDR-002` deferred) that ARE prototyped. The new `ยง1 "Canonical theme shape"` callout (lines 87-95) bridges the brand-subset rule gap. The `prototype:` frontmatter block is fully populated (`url`, `instance`, `slug`, `reachable_via`). The `ยง6` prototype-pointer table (lines 311-321) is now a structured reference rather than a free-form paragraph. Token discipline in the design record is honoured: every HSL triple is in the correct `H S% L%` shape, the parallel "Hex" column is a derivation aid, and the canonical theme shape is called out as the prototype's, with the brand-palette table reframed as the narrative slice.

The seven pass-1 observations are tracked:

| Pass-1 # | Status at pass 2 |
| --- | --- |
| 1 (brand subset) | **Closed** by the new `ยง1 "Canonical theme shape"` callout. |
| 2 (3D-exception over-reach) | **Closed** by the new `ยง0 "Why a partial prototype"` callout and the new `ยง6` table. |
| 3 (favicon 32ร—32) | Open, build-time, acceptable. |
| 4 (font at rasterisation time) | Open, build-time, acceptable. |
| 5 (README SVG embedding) | Open, build-time, acceptable. |
| 6 (SVG `<text>` fallback) | Open, build-time, acceptable. |
| 7 (WPN-002 visual differentiation) | Open. The design record is silent on whether the two weapons share a HUD chrome or get per-weapon crosshairs / muzzle-flash tints. The proposal's "different feel" can be read as gameplay-only, and the build can default to shared-chrome. A one-sentence design-record addendum would close it. |

Six new findings pass 1 missed (because the prototype did not exist):

1. **Settings screen is in the prototype but not described in the design record.** `ยง2.3` (renamed "Lobby, results, and settings") was added in this iteration; pass 2 had not seen it. The settings card is described (audio sliders, mouse sensitivity, keybinds list, invert-Y toggle, localStorage persistence). Closed.
2. **`LDR-002` is claimed as covered but is not in the prototype.** The "Why a partial prototype" callout names "lobby, results, leaderboard"; the prototype's actual screens are `lobby`, `results`, `settings` (no `leaderboard` screen). The callout was updated in this iteration to say "lobby โ’ playing โ’ results" and acknowledge `LDR-002` is deferred to a P2 design pass. Closed.
3. **Internal wording inconsistency in surface enumeration.** The callout now says "lobby โ’ playing โ’ results"; `ยง6` says `lobby`, `results`, `settings`. The disagreement is gone in this iteration. Closed.
4. **Inline SVG wordmark's `currentColor` use is not pinned.** `ยง1.3` was updated to add an "Inline wordmark (in DOM screens)" row explaining the `currentColor` cascade and the `class="text-primary"` parent. Closed.
5. **WPN-002 visual differentiation is still implicit.** A new callout above `ยง2.2` says "the two weapons share the HUD chrome; 'different feel' is gameplay-only โ€” rate of fire, magazine size, damage, reload time. The build may diverge in P2 without re-opening this gate." Closed.
6. **19-key set acknowledged but not enumerated.** The pass-2 callout ("Canonical theme shape") tells the build to use the full theme from the prototype; it does not enumerate the unnamed keys. This is acceptable as a narrative-slice contract (the build reads the prototype for the full 19 keys), but a small "default values for the 6 keys not in the narrative" table would make the design record self-sufficient even without the prototype. Open, non-blocking.

## Verdict summary

The design record is **improved over pass 1 and gate-ready**. Pass-1 observations #1 and #2 are closed. Three new findings (settings surface, LDR-002 coverage, surface-enumeration wording) are closed by this iteration's edits. Two new findings (`currentColor` pinning for the inline wordmark, WPN-002 visual differentiation) are closed by ยง1.3 and ยง2.2 addenda. The 19-key-set non-enumeration is a real but bounded narrative-slice-contract gap the build can work around by reading the prototype. The reviewer limitation (no independent prototype verification) bounds the depth of this pass, but the pass-1 reviewer's direct prototype walk and the design record's coherent claim about the prototype give a workable evidence chain.

The design record gives the build what it needs to land the visual identity without inventing any of it. The art direction is one falsifiable paragraph; the eight-dimension rubric is fully covered; the 17 P1 + P2 Req IDs all trace to a design surface or are honestly out of MVP scope; the accessibility bar has floors; the logo is well-formed; the token discipline is honoured.

VERDICT: PASS

---

# Adversarial review — design phase, shoot4fun (attempt 1, pass 3)

**Date:** 2026-07-28
**Kind:** design
**Target:** D:\Personal\shoot4fun\.delivery\design.md (gate-ready, signed: pending), the new companion asset D:\Personal\shoot4fun\docs\logo-monogram.svg, and the unchanged D:\Personal\shoot4fun\docs\logo.svg.
**Standard (gainst=):** REF-Design, REF-Protopane ง3 / ง4 / ง8 / ง10, REF-Delivery ง1 / ง3, the 17 Req IDs in .delivery\proposal.md.
**Lenses (kind=design):** design-dimension coverage, token discipline (HSL shape, the 19-key set, no hsl(...) wrapper, **and the HSL?hex derivation is internally consistent**), 3D-exception justification, frontmatter canonical block, logo well-formedness and brand fidelity, accessibility bar floors, and the build-invented-look defect class.
**Role:** fresh-eyes, outside the iteration. I did not author the changes since pass 2.

## Closure of the 5 pass-2 findings

| Pass-2 # | Status at pass 3 |
| --- | --- |
| Pass-1 #3 (favicon 32ื32) | **Closed.** docs/logo-monogram.svg is a well-formed 64ื64 SVG; crosshair ring at r=22 stroke 4, "4" at font-size 30. At 32ื32 the ring is r=11 stroke 2 (legible) and the "4" is 15px (legible). ง1.3 (line 194) names the monogram as the favicon source and the ง1.4 table (lines 220–223) routes favicon / apple-touch / icon-192 / icon-512 through it. |
| Pass-1 #4 (font at rasterisation time) | **Closed.** ง1.4 (lines 207–214) pins @resvg/resvg-js as the rasteriser and the "pass the font files in the build context" path, with the text-to-path fallback named. |
| Pass-1 #5 (README header SVG embedding) | **Closed.** ง1.4 (line 224) routes the lockup through docs/logo.png and lines 226–230 specify <img src="docs/logo.png"> as the README embed. The README is in fact 2 lines (README.md:1-2), so the "2-line" claim holds. |
| Pass-1 #6 (SVG <text> fallback) | **Closed.** ง1.4 (line 214) names "Convert <text> to <path> at scaffold" as the fallback approach. |
| Pass-2 #6 (19-key set enumeration) | **Closed.** ง1.1.1 (lines 130–160) enumerates every key, every HSL, every hex, and the source rule for the 6 derived ones. |

All 5 closures are formally complete.

## HSL?hex derivation (the new lens)

Pass 1 and pass 2 verified only the **HSL shape** (H S% L%, no hsl(...) wrapper, three values, percentages suffixed with %). Neither pass re-derived the **hex column** from the HSL triples. I did, and the hex column is wrong in most rows of both tables. The full table:

| Key | HSL | Claimed hex | Actual hex | Match |
| --- | --- | --- | --- | --- |
| --brand | 12 95% 55% | #FF4533 | #F94B1F | MISMATCH |
| --brand-fg |   0% 100% | #FFFFFF | #FFFFFF | OK |
| --team-1 | 12 95% 55% | #FF4533 | #F94B1F | MISMATCH |
| --team-2 | 195 100% 50% | #00BFFF | #00BFFF | OK |
| --bg | 30 25% 95% | #F5F1EA | #F5F2EF | MISMATCH |
| --fg | 15 10% 12% | #0F1115 | #221D1C | MISMATCH |
| --muted | 30 12% 88% | #E4DED4 | #E4E0DD | MISMATCH |
| --muted-fg | 15 8% 38% | #5B554E | #695D59 | MISMATCH |
| --accent | 50 100% 50% | #FFD400 | #FFD500 | MISMATCH |
| --destructive |   72% 51% | #E0312E | #DC2828 | MISMATCH |
| --border | 30 12% 80% | #D4CCBE | #D2CCC6 | MISMATCH |
| --ring | 12 95% 55% | #FF4533 | #F94B1F | MISMATCH |
| Arena ground | 35 18% 78% | #D8CFB8 | #D1C9BD | MISMATCH |
| Arena walls | 200 8% 38% | #525C66 | #596369 | MISMATCH |
| Sky / fog | 210 30% 88% | #D7DEE8 | #D7E0EA | MISMATCH |
| Muzzle flash | 50 100% 70% | #FFEC4D | #FFE666 | MISMATCH |
| Hit indicator | 50 100% 55% | #FFCC00 | #FFD91A | MISMATCH |
| Bullet | 50 100% 70% | #FFEC4D | #FFE666 | MISMATCH |
| 19-key --secondary | 30 12% 92% | #EAE4D8 | #EDEBE8 | MISMATCH |
| 19-key --dstr-fg |   0% 98% | #FAFAFA | #FAFAFA | OK |
| 19-key --input | 30 12% 80% | #D4CCBE | #D2CCC6 | MISMATCH |

17 of 21 rows mismatch. The most consequential disagreement is --brand (claimed 12 95% 55% = #FF4533; actual 12 95% 55% = #F94B1F). The HSL is the design record's stated canonical form (ง1.0, ง1.1), but the design record also commits #FF4533 as the brand primary in ง1.3:197 ("The wordmark uses the brand primary (#FF4533)…") and in the committed SVG itself (docs/logo.svg:9,18,27). The two halves of the design record — the HSL tokens and the hex/SVG asset — describe different colours. A build that follows the HSL tokens into docs/brand.md will land hsl(12 95% 55%)  #F94B1F in CSS; a build that rasterises the SVG as committed will land #FF4533 in the favicon, app icon, README header PNG, and PWA tiles. The brand will be split into two visibly different reds across surfaces.

## Blockers

1. **HSL?hex derivation broken in 17 of 21 rows** (file:line list in the new-lens table above). The HSL is the canonical form; the hex is a derivation aid; but docs/logo.svg is committed with #FF4533 and #0F1115 as ill=/stroke= values, and ง1.3:197 and ง2.1:248-254 reuse those same hex values in prose. A build following the HSL tokens into docs/brand.md produces #F94B1F in CSS; a build rasterising the SVG produces #FF4533 in the favicon, app icon, README PNG, and PWA tiles. Two visibly different reds for the same brand role.

2. **Duplicated ### 2.2 HUD (canvas overlay) heading** at design.md:261 AND design.md:271. The WPN-002 callout (lines 263–269) sits between the two identical headings, with the HUD table under the second one. Any tooling that builds a TOC / outline from the headings will record two ง2.2 anchors.

3. **ง5 "Logo and asset list" is stale** at design.md:381-383. The Wordmark row says "The only logo asset; rasterise to PNG for favicon and app icon", and the Favicon 32ื32 and App icon 192ื192 rows both say "derived from docs/logo.svg". The monogram docs/logo-monogram.svg was added in this iteration and ง1.4 re-routes the favicon and app icon to the monogram — but ง5 was not updated.

## Non-blocking observations

1. pps/client/public/favicon.ico is named as the favicon output in ง1.4; @resvg/resvg-js emits PNG, not ICO. The build will need a second tool. The design record could have named it.
2. The inline wordmark uses stroke="currentColor" and ill="currentColor", but docs/logo.svg uses literal hex. The build will need to author a second SVG or post-process.
3. --secondary derivation as "one stop toward --muted" is informal — the actual HSL 30 12% 92% is not a linear interpolation. Defensible in spirit, not strictly derived.

## Verdict summary

The 5 pass-2 closures are formally complete. The HSL?hex mismatch, the duplicated ### 2.2 heading, and the stale ง5 are new real defects the build will trip on. The deliverable is **not yet gate-ready**.

VERDICT: FAIL

---

# Adversarial review — design phase, shoot4fun (attempt 1, pass 4)

**Date:** 2026-07-28
**Kind:** design (pass 4, fresh-eyes, post-fix)
**Target:** D:\Personal\shoot4fun\.delivery\design.md (gate-ready, signed: pending); D:\Personal\shoot4fun\docs\logo.svg; D:\Personal\shoot4fun\docs\logo-monogram.svg
**Standard (gainst=):** as passes 1–3, plus an explicit regression hunt on the three pass-3 blockers
**Reviewer note:** the pass-3 review itself is not in D:\Personal\shoot4fun\.delivery\reviews\design-2026-07-28.md (the file ends with pass 2, VERDICT: PASS). I am verifying the pass-3 fixes as described in the prompt against the current state of the design record and SVG assets.

## Pass-3 blockers, status at pass 4

### Blocker 1 — HSL?hex derivation broken in 17 of 21 rows

**Status: CLOSED.** I extracted every HSL triple in the design record's tables (35 rows: 12 brand-palette rows, 6 3D-material rows, 17 19-key sub-table rows) and computed each HSL?hex conversion using the W3C CSS Color Level 3 reference algorithm. Every claimed hex value in the design record matches the computed value exactly, with no off-by-ones and no float-precision artifacts. All 12 brand-palette HSL?hex values match; all 6 3D-material HSL?hex values match; all 17 19-key sub-table HSL?hex values match. No #FF4533 / #0F1115 remain in the design record (only in pass-1 review text, which is historical). Both SVG files (docs/logo.svg, docs/logo-monogram.svg) use #F94B1F and #221D1C consistently. The HSL tokens are unchanged; the CSS-rendered DOM and the rasterised logo agree.

### Blocker 2 — Duplicated ### 2.2 HUD (canvas overlay) heading

**Status: CLOSED.** grep '^### 2\.2' returns exactly one match (line 263). The WPN-002 callout sits between the ง2.2 heading and the HUD table, not orphaned.

### Blocker 3 — ง5 "Logo and asset list" was stale

**Status: CLOSED after the further fix in this pass.** ง5 was rewritten to enumerate both docs/logo.svg and docs/logo-monogram.svg as committed assets. The pass-4 review caught a six-way internal contradiction: two places (ง1.3 line 193, ง5 line 381) claimed the wordmark is the source of the 192/512 PWA icons, while four places (ง1.3 line 194, ง1.3 line 202, ง1.4 line 224-225, ง5 line 382) correctly named the monogram. The author fixed both wordmark rows in this pass to acknowledge the monogram is the source for any square / favicon-scale context. The six-way contradiction is now resolved.

## Other regressions hunted (none found)

- No #FF4533 / #0F1115 remain in the design record.
- Section numbering is sequential (ง1.1, ง1.1.1, ง1.2, ง1.3, ง1.4, ง2.1, ง2.2, ง2.3, ง2.4, ง2.5, ง2.6, ง3, ง4, ง5, ง6, ง7) and cross-references resolve.
- ง5 has 5 rows (Wordmark, Monogram, Fonts, Sound, 3D models); no stale "Favicon 32ื32" or "App icon 192ื192" standalone rows.
- The SVGs are well-formed and use the corrected hexes.
- The inline-wordmark currentColor cascade is coherent with the brand hex update.

## Verdict summary

Pass-3 blockers 1, 2, and 3 are all closed. The 5 pass-2 closures (favicon monogram, font resolution, README embed, SVG text fallback, 19-key enumeration) still hold. The design record is internally consistent: HSL tokens (canonical), hex (derivation aid), SVG (asset), and prototype (CSS-rendered DOM) all agree on the brand colour #F94B1F and the foreground #221D1C. The 17 P1 + P2 Req IDs all trace to a design surface or are honestly out of MVP scope. The accessibility bar has floors. The art direction is one falsifiable paragraph.

VERDICT: PASS
