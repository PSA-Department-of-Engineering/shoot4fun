# Fresh-eyes adversarial review - build (shop surface), 2026-08-22

Reviewer: fresh-context subagent (no share of the builder's context).
Target: the shop-surface feature, `delivery/2026-08-21` vs `origin/main`
(git diff origin/main...HEAD -- backend apps/client/src apps/client/e2e).
Against: intent.yaml claims INT-030..INT-038; ADR-0007 + ADR-0008;
handoff §2 envelope conventions and §7 repo contract; brand token
discipline.

This file records the closing pass verbatim. Two earlier passes found
blockers (an unguarded concurrent acquire; then a lock registry that
per-request construction silently bypassed, plus a vacuous regression
test); each was fixed and re-reviewed before this one.

---

## Closing adversarial review — shop-surface line (`delivery/2026-08-21` vs `origin/main`)

### Prior blockers — verified fixed

**B1-v2 (write serialization)** — VERIFIED FIXED.
- `container.py:83-89` owns ONE `ShopWriteLocks` registry and TWO singleton use cases (`acquire_item`/`equip_cosmetic`) sharing it; accessors return the same instances.
- Both writers wrap the whole read-modify-write in `async with self._locks.for_user(user_id)` (`acquire_item.py:90`, `equip_cosmetic.py:49`); catalog check before locking is harmless.
- Lock-scheme audit for deadlock/release races: `release_user` only ever runs inside the held critical section, where `locked()` is True, so it can never pop a contended entry or strand a waiter; during the release→wake window asyncio.Lock's `all(waiters cancelled())` guard prevents a newcomer from bypassing queued tasks onto the same object; one lock per operation, no nesting → no deadlock possible.
- Regression test `test_shop_entitlement.py:67-126` wires two `AcquireItem` instances over ONE shared registry with a yield-injecting repo stub and asserts both different-item acquires persist; without locks the interleaving loses a record, so the test genuinely discriminates.

**N-fixes** — ALL VERIFIED: honest stamping (`cosmetics.ts:63` sets `data-equipped-skin` only when a mesh actually painted; `RigView.tsx:124-139` clears it when cosmetic cleared); `BRAND.secondary` added to tokens.ts matching theme.css `--secondary: 30 12% 92%`; unlock errors surfaced with disabled-until-done button (`ShopItemDetail.tsx:106-119`, `role="alert"`); sticky error cleared on selectItem (`shop.state.ts:58`).

### Claim/contract/convention sweep
- INT-030..038: all nine `active` with markers — e2e INT-030/031(×2)/034(e2e half)/036 in `shop.spec.ts`; pytest INT-032/033/034/035/037/038 across five test files. Suite green.
- ADR-0007/0008 + handoff §2: writes ride `ArsenalEnvelope.parse`→copy→whole save; `version` preserved; `loadout` inside `data`; ownership = `{id, acquired_at}` inventory entries; legacy strings survive; INT-038 tests run real use cases against over-shaped payloads.
- Brand discipline: zero literal colors in the entire client diff (all `hsl(var(--token))`; swatch fills resolve via token maps); catalog specs are token-backed and validated server-side against the closed five-tier set; legend matches docs/brand.md.
- §7 repo contract: conventional commits throughout, no co-author trailers (`delivery:` type has direct precedent on main); no changes to compose file, CI caller, or pre-commit config; Dockerfile `COPY catalog /app/catalog` matches `_CATALOG_PATH` under uvicorn's cwd sys.path insertion.

### Verification runs
- backend: **118 passed, 2 skipped** (both pre-existing postgres-gated, not shop)
- client: `tsc --noEmit` clean; vitest **83 passed**

### Non-blocking findings
1. **Dead cleanup path / misleading docstring** (`acquire_item.py:67-72`): `release_user` can never pop at either call site (both hold the lock when calling it), so "the map does not grow with the account table" never happens — one tiny entry accumulates per shopping account per process. Cosmetic; no correctness impact. *(Resolved post-review: docstring corrected to describe the actual behavior; no code change needed for correctness.)*
2. **Process-scope of serialization undocumented** (ADR-0008): mutual exclusion holds per process; correct as deployed (single uvicorn worker) and as claimed, but >1 replica/worker would silently reintroduce lost whole-envelope upserts. Worth one consequence-line in ADR-0008 if scaling ever changes. *(Resolved post-review: consequence line added to ADR-0008.)*

BLOCKERS: none.

VERDICT: PASS
