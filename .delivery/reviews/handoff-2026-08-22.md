# Fresh-eyes adversarial review — handoff.md (plan gate), 2026-08-22

> Three independent fresh-context rounds were run against this handoff and its
> claims. Round 1: VERDICT FAIL (1 blocker, 1 major, 4 minors) — all fixed.
> Round 2: VERDICT PASS (1 major, 2 minors carried) — all fixed. Round 3's
> verbatim output is preserved below; its single MINOR was resolved immediately
> after with the reviewer's own prescribed sentence (noted at the end).
> Rounds 1–2 outputs are superseded; their findings are itemized in
> `.delivery/journal.md`.

---

## Round 3 — verbatim

**Fix verification**

1. **INT-034 scoping + dual attestation** — VERIFIED. intent.yaml INT-034 (lines 556–557): statement splits persistence ("survives sign-out and sign-in on any client") from "the acquisition visibly populates the Arsenal inventory surface," and the rationale pins dual attestation verbatim: "the persistence half is integration-scoped below; the visible-population half carries an additional e2e marker on the unlock journey (shop.spec.ts asserts the inventory lists the acquired item), since rendering is not attestable from pytest."

2. **Handoff §2 top-level loadout warning** — VERIFIED. handoff.md:82: "a top-level `loadout` field exists in `accountApi.ts`'s `ArsenalView` type, but ADR-0007's preservation contract covers keys *inside `data`* only — write `loadout` inside `data`, never as a sibling of it; a top-level key would be silently dropped by round-trips." Factually grounded: `ArsenalView` indeed has a top-level `loadout` (accountApi.ts:156–161), distinct from the envelope's `data.inventory` (arsenal.state.ts:11–24).

3. **INT-036 Acquired-screen pinning** — VERIFIED. intent.yaml:581: "The Acquired screen mounts the same RigView molecule to show the just-applied skin - one rig surface, reused, not a second renderer." Matches handoff §3 layout (`ui/shop/ … (Acquired reuses RigView)`, :107) and §6 step 4 ("the Acquired screen reuses the `RigView` molecule", :139). All three documents agree.

**New findings**

1. **MINOR** — handoff.md §5 (:132) vs §6 steps 3–4 (:138–139) vs intent.yaml INT-034: activation-ordering gap unique to INT-034. §5's rule is "implement the test, see it pass, then activate the claim," but §6 sequences integration tests (step 3) *before* the e2e specs (step 4). Following §5 literally, a builder activates INT-034 at step 3 — leaving its visible-population half (now explicitly part of the statement) attestable by nothing until step 4. INT-034 is the only claim whose attestation spans both steps; the handoff never says to defer its activation until the shop.spec.ts marker exists. Final state self-heals, but mid-run the claim is active with a half-untested statement. One sentence in §6 step 3 ("activate INT-034 only after step 4's e2e marker passes") closes it.

No contradictions between the edited documents were found otherwise; the §2 conventions, §3 sketch, §6 sequence, and intent.yaml rationales are mutually consistent.

VERDICT: PASS

---

## Post-review resolution (recorded by intent-session, 2026-08-22)

Round 3's MINOR was resolved by adding exactly the reviewer's prescribed sentence
to §6 step 3: *"Activate INT-034 only after step 4's e2e marker passes (its
statement spans persistence and visible population)."* No other change was made
after the reviewed state above.
