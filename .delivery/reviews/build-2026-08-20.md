# Fresh-eyes adversarial review — build & ship (issue #41: Login, Profile & Arsenal)

- Date: 2026-08-20
- Phase: run-delivery-plan (build-ship)
- Target: working tree of `delivery/shoot4fun-login-profile` (uncommitted build + new files)
- Against: `intent.yaml` INT-023…INT-029, `.delivery/handoff.md` (authorization + build sequence), REF-Foundry repo contract, visual identity
- Reviewer: fresh-context subagent (no producer context)

## VERDICT: PASS

No blockers. Every active claim (INT-023…INT-029) is named by a real passing
test marker; the shipped gameplay / identity claims (INT-001…INT-022) were not
modified; no foundry contract file (`foundry.yaml`, `teams/`, `k8s/`) was
touched; the Arsenal PUT validates server-side through `ArsenalEnvelope.parse`
and returns 400 on ill-formed input; unknown `data` fields round-trip; and the
reconciliation loop is correctly wired (`pullFromAccount` invoked on `signIn`
at `account.actions.ts:132`; `pushToAccount` gated on `registered` and writing
all five prefs at `settings.actions.ts:167-177`).

## Non-blocking observations

1. `postgres_account_repository.py` `get_arsenal` decoded a `jsonb` column with
   `json.loads`; asyncpg already decodes it to a dict, so a `TypeError` would
   surface under `TEST_DATABASE_URL`. Fixed in this build: decode only when the
   value is a `str`. (In-memory contract path unaffected.)
2. `intent.yaml` INT-023…INT-029 comment blocks still read "Draft because no
   marker names it"; the claims are now active. Comment text updated to reflect
   active status (attestation unaffected).
3. `RigView.tsx` instantiates a second `THREE.WebGLRenderer` alongside the app's
   global `SceneStage` renderer; two live WebGL contexts while the Arsenal view
   is open. Within browser limits; revisit when the full 3D viewer lands.
4. `Arsenal.tsx` `data-arsenal-model-name` ("robot") is the client's hardcoded
   `DEFAULT_ARSENAL` default, not read from the loaded `CharacterLibrary`
   instance. Matches the documented placeholder scope (ARS-002); INT-027 asserts
   only the placeholder name.
5. `accounts.py:205` `get_arsenal` empty fallback returns `{"version":1,"data":{}}`,
   which would downgrade a future stored v2 envelope to v1 on the empty branch.
   Harmless today (no writes yet).

## Verification evidence (build run)

- Backend pytest: 93 passed, 1 skipped.
- Client `tsc --noEmit`: clean.
- Client vitest: 83 passed.
- Playwright e2e: 18 passed (account.spec.ts INT-023/024/025, arsenal.spec.ts
  INT-026/027/028, plus match/touch/smoke/typography suites).
- `csd-intent` audit: CLEAN (29 claims, 24 attested; INT-023…INT-029 all active
  and marked).

VERDICT: PASS
